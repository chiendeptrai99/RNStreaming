import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Executed = () => {
    return (
        <View style = {{flex: 1, justifyContent:"center", alignItems: 'center', backgroundColor: '#12141F'}}>
            <Text>Executed</Text>
        </View>
    )
}

export default Executed

const styles = StyleSheet.create({})
